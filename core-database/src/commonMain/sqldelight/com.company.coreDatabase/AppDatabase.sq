CREATE TABLE game(
id INTEGER NOT NULL PRIMARY KEY,
name TEXT NOT NULL,
image TEXT NOT NULL);


upsert:
INSERT OR REPLACE INTO game(id,name,image) VALUES(?,?,?);

getAllGames:
SELECT * FROM game;

delete:
DELETE FROM game WHERE id=?;